env="${1:-sandbox}"
echo 'MONEY PRINTER GOING BRRR'
echo "BRRRing in $env"
export MONEY_PRINTER_ENV=$env && (trap 'kill 0' SIGINT; (./bin/data-server $env & ./bin/client-bus $env & ./bin/task-runner $env & ./bin/api $env) > /dev/null)