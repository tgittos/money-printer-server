#!/bin/sh

env="${1:-development}"
echo 'MONEY PRINTER GOING BRRR'
echo "BRRRing in $env"
export MP_ENVIRONMENT=$env && (trap 'kill 0' SIGINT; (./bin/data-server $env & ./bin/client-bus $env & ./bin/task-runner $env & ./bin/api $env) > /dev/null)